Focus in the directory exploration_policies/toy_v1/.
I've copied the code from exploration_policies/toy_v0/ there.

I want you to change it in the following ways:

* Add actions to the tasks.  I want all tasks to have an action space with actions
`left` and `right` that move the vector to the left and/or right, respectively,
cutting off and filling in as appropriate.  Also allow a do-nothing action.

* Policy.  I want a basic policy that on every timestep, with probability p
samples one of the left/right actions, and with probability 1 - p does nothing,
where p is a parameter called `action_probability`.  Organize the code well, e.g.
by using a mixin or wrapper class for the policy.  I may add other policies in
the future.

* For the models, make each layer receive an action as input for its state update
using a one-hot code for the action identity (where do-nothing is all zeros).
I want the state update to be
h(t + 1) = tau * h(t) + tanh((1 - tau) * encoder(h(t) + noise, input(t), action(t)))
and I want the action to be executed in the subsequent timestep, namely modifying
input(t+1).

* I also want to have an option to add feedback for the action in the input.
Specifically, append the one-hot action to the input vector for the step after
the action has been executed.  But make this optional.

Figure out how to organize the code cleanly with this changes.

Also add a demo notebook that shows the effects of this policy and actions on the
tasks, and update the model-training notebook to incorporate these actions.

Lastly, a reminder to work entirely in toy_v1/.
